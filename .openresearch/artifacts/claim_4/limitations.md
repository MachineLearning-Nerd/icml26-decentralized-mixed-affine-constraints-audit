# Claim 4 limitations

The benchmarks are seeded synthetic linear-learning tasks rather than production deployments. The paper reports formulations, not target accuracy numbers, so predictive MSE is a non-vacuity diagnostic rather than a reproduced benchmark. Appendix B MTL is coupled-only; the node-specific affine mask is a disclosed extension, limiting confidence to MEDIUM.
