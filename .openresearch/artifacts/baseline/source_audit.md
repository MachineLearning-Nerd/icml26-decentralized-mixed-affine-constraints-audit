# Baseline source audit

The retrieved ar5iv HTML is pinned by SHA-256 in `claim_contract.json`. Claim 1 is corrected to the paper's actual scope: Theorem 4.1 concerns shared-variable constraints (problem 10); Theorem 4.6 concerns the full mixed problem (P). The universal optimality statements are not considered verified by finite experiments.

