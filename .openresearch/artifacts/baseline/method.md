# Baseline method

This node performs no scientific experiment. It checks that the empty starting repository is represented honestly as five `BLOCKED` claims, prints the immutable source pin, fixed command, Git SHA, environment versions, requested HF CPU flavor, logical CPU allocation, and runtime, and exits nonzero if any baseline claim is improperly upgraded.

