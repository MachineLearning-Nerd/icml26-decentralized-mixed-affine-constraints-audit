# Claim 5 method

The clean-room implementation follows Algorithm 1 in source order. The hard-case control replaces `z^(k+1)` with `z^k` only in the corrector line while keeping data, initialization, parameters, and budget fixed.
